# DO THESE FIRST!!
1. Run `haxelib --global install hmm` and after than `haxelib --global run hmm setup` then run `hmm install`
this will install all the needed dependencies

# Tips (Optional)
1. i don't know if theres a better way but after running the hmm i would recommend going in .haxelib/flxanimate/4,0,0/flxanimate/animate/FlxSymbolDictionary.hx and then commenting out line 129 if you dont want it to print all the animations when using flxanimate